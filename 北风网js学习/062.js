window.onload=function(){
	var box=document.getElementById('box');
	
	/*
		box.getBroundingClientRect();得到矩形对象
		box.getBroundingClientRect().top;
		box.getBroundingClientRect().left;
		box.getBroundingClientRect().bottom;
		box.getBroundingClientRect().right;
*/

动态脚本，设置开关 加载script
动态样式，设置开关 加载link

动态加载style

设置开关加载标签
}